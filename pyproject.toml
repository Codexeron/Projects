[project]
name = "mimar-link-checker"
version = "2.0.1"
description = "Mimar Link Checker - Secure and Fast"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["aiohttp>=3.9.0"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
